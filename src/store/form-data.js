import {atom} from 'jotai';

const FormCategory = atom('')
const FormDescription = atom('')
const FormAttachFile = atom('')
const FormCheckLocation = atom('')
const FormDescribeLocation = atom('')
const FormSwearCheck = atom('')
const FormImageUrl = atom('')
